import { defineMessages } from 'react-intl'

export const scope = 'shop.components.Molecules.LocaleToggle'

export default defineMessages({
  en: {
    id: `${scope}.en`,
    defaultMessage: 'en'
  },
  ja: {
    id: `${scope}.ja`,
    defaultMessage: 'ja'
  },
  nl: {
    id: `${scope}.nl`,
    defaultMessage: 'nl'
  }
})
